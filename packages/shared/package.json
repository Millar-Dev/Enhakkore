{
  "name": "@enhakkore/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain model, money primitives and constants for the Enhakkore platform.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "//prepare": "npm runs this automatically after any install, so `dist` exists before anything imports it. Without it a fresh clone — and any CI or hosting build — fails to resolve @enhakkore/shared.",
    "prepare": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
